{
  "name": "chatbot",
  "version": "1.0.0",
  "description": "chatbot for class reminder",
  "main": "dist/main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": " esbuild src/main.ts --bundle --platform=node --packages=external --outdir=dist/bundle/ --format=esm"
  },
  "type": "module",
  "author": "M Afif Ramadhan",
  "license": "GPL-3.0",
  "devDependencies": {
    "@types/luxon": "^3.3.8",
    "@types/node-cron": "^3.0.11",
    "date-fns": "^3.0.6",
    "esbuild": "0.19.11",
    "nodemon": "^3.0.2",
    "pocketbase": "^0.20.1",
    "typescript": "^5.3.3",
    "venom-bot": "^5.0.21"
  },
  "dependencies": {
    "dotenv": "^16.3.1",
    "luxon": "^3.4.4",
    "node-cron": "^3.0.3"
  }
}
